// import mongoose, { Schema } from "mongoose";
// export const WishItemsSchema = new Schema(
// 	{
// 		wishList: {
// 			type: String,
// 		},

// 		userId: { type: mongoose.Schema.Types.ObjectId, ref: "user" },
// 	},
// 	{ timestamps: true }
// );

// export const WishModel = mongoose.model("wishList", WishItemsSchema);
